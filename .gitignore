.bundle/
_site/
vendor/
Gemfile.lock
*~
.jekyll-metadata
node_modules/

# Fork-private planning notes — kept local, not part of the repo history.
planning/

# Parity-harness failure artifacts: written next to each fixture on mismatch.
tests/fixtures/*/actual.svg

# Build outputs. docs/typograms.js is regenerated by `npm run build` but
# stays tracked because GitHub Pages serves it directly from the repo.
dist/
